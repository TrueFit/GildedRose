{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/GRBusinessLogic.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}